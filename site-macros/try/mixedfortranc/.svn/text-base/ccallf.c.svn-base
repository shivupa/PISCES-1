#include <stdlib.h>
#include <string.h>

void showmsg_( const char* c, int len );

int main() {
   
   const char* msg = "ccallf success";
   int len = strlen(msg);
   showmsg_( msg, len );

   return 0;
}
